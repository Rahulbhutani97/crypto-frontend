import { AppOnlyNumberDirective } from './app-only-number.directive';

describe('AppOnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
