import { CuttingNumberOnpasteDirective } from './cutting-number-onpaste.directive';

describe('CuttingNumberOnpasteDirective', () => {
  it('should create an instance', () => {
    const directive = new CuttingNumberOnpasteDirective();
    expect(directive).toBeTruthy();
  });
});
